#include<stdio.h>
#include<math.h>
int sin(double degree)
{
    int degree,answer;

    answer=sin(degree);

    return answer;
}

int cos(double degree)
{
    int degree,answer;

    answer=cos(degree);

    return answer;
}

int tan(double degree)
{
    int degree,answer;

    answer=tan(degree);

    return answer;
}